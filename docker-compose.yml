version: '3.8'

services:
  # OpenMetaMind Streamlit UI (runs swarm directly)
  streamlit:
    build:
      context: .
      dockerfile: Dockerfile.streamlit
    ports:
      - "8501:8501"
    environment:
      - ENV_FILE=/app/.env
    env_file:
      - .env
    volumes:
      - ./.env:/app/.env:ro
    networks:
      - openmetamind-network
    restart: unless-stopped

  # Optional: PostgreSQL for production checkpointing
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: openmetamind
      POSTGRES_USER: openmetamind
      POSTGRES_PASSWORD: openmetamind_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - openmetamind-network
    restart: unless-stopped

networks:
  openmetamind-network:
    driver: bridge

volumes:
  postgres_data: